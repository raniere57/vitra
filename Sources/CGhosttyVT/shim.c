// libghostty-vt is a pure C library consumed through its headers; this
// translation unit exists only because SwiftPM requires a C target to have at
// least one source file.
#include <ghostty/vt.h>
